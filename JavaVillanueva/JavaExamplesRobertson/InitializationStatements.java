package JavaExamplesRobertson;

public class InitializationStatements {
    public static void main(String[] args) {
        int totalScore = 0;
        int maxScore = 300;

        if(maxScore < totalScore);
    }
}
